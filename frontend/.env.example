# Copy to .env.local for local dev. Never commit .env.local.
# In local dev this can be left unset — Vite proxies /api → localhost:8000 automatically.
# Set in Vercel environment variables for production.
# VITE_API_URL=https://cbc-guidance-api.azurewebsites.net/api/v1
